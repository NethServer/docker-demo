This is a demo of NethServer. 
The only working part is the web interface. No installed module will work.

Official site: www.nethserver.org



To restart the all docker instances:
  docker stop $(docker ps -q -a);  docker rm $(docker ps -q -a);  docker run -d -p 8080:980 gsanchietti/nethserver-demo:latest
